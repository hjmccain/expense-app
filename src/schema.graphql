type Query {
  test: String
  expenses: [Expense!]!
}

type Mutation {
  post(
    total: Int!,
    description: String!,
  ): Expense!
}

type Expense {
  id: ID!
  total: Int!
  description: String!
}